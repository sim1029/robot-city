# Assistant benchmarks

No benchmark result has been published yet. Run the complete live calendar suite and use `npm run eval:publish -- data/evaluations/<result>.json` to add the first documented configuration score.
